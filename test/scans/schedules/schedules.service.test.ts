import { SchedulesService } from 'src/scans/schedules/schedules.service';

describe('SchedulesService', () => {
  it('should be defined', () => {
    expect(new SchedulesService()).toBeDefined();
  });
});
